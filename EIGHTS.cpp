#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    long t;
    cin >> t;
    while(t--)
    {
        long long k;
        cin >> k;

        long th = 0;

        cout << 192 + (250 * (k - 1)) << endl;
        
    }
    return 0;
}